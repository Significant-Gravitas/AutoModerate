# Flask and web framework
Flask[async]==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.2
Flask-SocketIO==5.6.1
WTForms==3.2.1
Werkzeug==3.1.8

# OpenAI integration
# Latest 2.x — reasoning_effort is supported since 1.58, and the only 2.0
# breaking change was in the Responses API (ResponseFunctionToolCallOutputItem)
# which we don't use. chat.completions is unchanged.
openai>=2.44.0,<3.0.0
tiktoken==0.12.0
httpx[http2]==0.28.1

# Security and utilities
bcrypt==5.0.0
python-dotenv==1.2.2
requests==2.33.1
urllib3>=2.6.3  # Security fix for CVE-2024-37891
pre-commit==4.5.1
pydantic==2.13.2
Flask-Talisman==1.1.0
Flask-Limiter==4.1.1
Authlib==1.7.0
psutil==7.2.2
# >=2.40 needed for `enable_logs=True` kwarg on sentry_sdk.init()
sentry-sdk[flask]>=2.64.0,<3.0.0
regex==2024.11.6  # ReDoS-safe regex with timeout support

# Production WSGI
# gthread worker — compatible with Flask async routes + asgiref.AsyncToSync.
# See wsgi.py for the full rationale on why we ditched eventlet here.
gunicorn==26.0.0

# Database support
psycopg2-binary==2.9.11

# BeautifulSoup
beautifulsoup4==4.14.3

# Async support
asyncpg==0.31.0
